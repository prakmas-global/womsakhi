"use client";

import { Hub } from "@/components/ux/nav/Hub";

/** The account section's map — see `components/ux/nav/Hub.tsx` for why it is a
 *  page rather than a menu. */
export default function YouHub() {
  return <Hub id="account" />;
}
