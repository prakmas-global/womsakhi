"use client";

import { Hub } from "@/components/ux/nav/Hub";

/** The help section's map — see `components/ux/nav/Hub.tsx` for why it is a
 *  page rather than a menu. */
export default function HelpdeskHub() {
  return <Hub id="help" />;
}
