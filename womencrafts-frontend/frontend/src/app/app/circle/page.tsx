"use client";

import { Hub } from "@/components/ux/nav/Hub";

/** The circle section's map — see `components/ux/nav/Hub.tsx` for why it is a
 *  page rather than a menu. */
export default function CircleHub() {
  return <Hub id="circle" />;
}
